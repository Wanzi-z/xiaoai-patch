--- a/etc/init.d/dhcpc	2018-06-14 04:18:31.000000000 +0200
+++ b/etc/init.d/dhcpc	2022-07-24 17:17:51.470951700 +0200
@@ -9,15 +9,12 @@
     logger -t wireless -p 3 "${MICO_SYSLOG_BINDDEVICE} $*"
 }
 
-
 start_service() {
-  ROM_TYPE=$(uci -c /usr/share/mico get version.version.HARDWARE)
-  h_name="MiAiSoundbox-${ROM_TYPE}"
-  case $ROM_TYPE in
-    S12|S12A|s12|s12a)
-        h_name="MiAiSoundbox"
-    ;;
-  esac
+  h_name=$(cat /proc/sys/kernel/hostname)
+  if [ "$h_name" = "mico" ]; then
+    ROM_TYPE=$(uci -c /usr/share/mico get version.version.HARDWARE)
+    h_name="MiAiSoundbox-${ROM_TYPE}"
+  fi
   
   procd_open_instance
   procd_set_param command udhcpc -f -s /bin/simple_dhcp.sh -R -t 0 -i wlan0 -x "hostname:$h_name"
